'use strict';

/*========= MAP ===========

-- Array.map()
Great in creating a new array with changed values.

Example:
const names = ['justin', 'ViVIAN', 'boB', 'DANIEL', 'tRant'];

old way:
const newNames = [];
newNames.push(convertToTitleCase(names[0]));
new


---- Array.filter()
Great for removing elements in an array that fail a given test.

Example:
const names = [Justin, Vivian, Bob, Daniel, Trant];
const output - names.filter(name => name !== Bob && name !== Steve); this says filter out Bob
console.log(output); // [Justin, Vivian, Daniel, Trant]

----- Array.reduce()
Great for combining the values of an array together in some way to create data of any type.

 */